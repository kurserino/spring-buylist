{
  "scripts": {
    "build": "gulp sass && gulp babel",
    "install": "mvn dependency:resolve && npm run build",
    "start": "npm run build && mvn spring-boot:run"
  },
  "dependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.3",
    "@babel/preset-env": "^7.2.3",
    "gulp": "^4.0.0",
    "gulp-babel": "^8.0.0",
    "gulp-concat": "^2.6.1",
    "gulp-sass": "^4.0.2",
    "gulp-uglify": "^3.0.1"
  }
}
